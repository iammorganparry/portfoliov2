import { Button, Card, styled } from "@nextui-org/react";
import { motion } from "framer-motion";

export const HoverCard = styled(motion(Card))
export const MotionButton = styled(motion(Button), {})
